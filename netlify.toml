[build]
  base = "client"
  command = "npm run build"
  publish = "dist"

# Redirect all routes to index.html so React Router handles client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
